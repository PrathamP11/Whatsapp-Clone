{
  "name": "whatsapp-clone-backend",
  "version": "1.0.0",
  "description": "WhatsApp Web Clone Backend",
  "main": "server.js",
  "scripts": {
    "build": "cd ../frontend && npm install && npm run build",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "process-payloads": "node scripts/processPayloads.js",
    "clear-db": "node scripts/clearDatabase.js",
    "reset-db": "npm run clear-db && npm run process-payloads"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "socket.io": "^4.7.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}